<UserControl
    x:Class="DesktopApplication.Views.EditSimulationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:properties="clr-namespace:DesktopApplication.Properties"
    xmlns:viewModels="clr-namespace:DesktopApplication.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:ConfigurationEditorViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/StylesDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource HeaderTextBlockStyle}" Text="{x:Static properties:Resources.Name}" />

        <TextBlock Grid.Row="1" Text="{x:Static properties:Resources.EditSimulationInfoText}" />

        <StackPanel
            Grid.Row="2"
            VerticalAlignment="Center"
            Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center" Text="{x:Static properties:Resources.ConfigurationName}" />
            <TextBox
                Width="192"
                Margin="16,0,0,0"
                Text="{Binding Path=Value.Name, Mode=TwoWay}" />
        </StackPanel>
    </Grid>
</UserControl>